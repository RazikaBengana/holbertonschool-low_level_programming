#include "main.h"

/**
 * add - program that adds two integers and returns the result
 *
 * @m: the 1st integer
 * @n: the 2nd integer
 *
 * Return: the result of the sum
 */

int add(int m, int n)
{
	return (m + n);
}